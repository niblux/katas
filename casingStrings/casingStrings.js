String.prototype.toJadenCase = function (quote) {
    //...
    // convert into an array, by splicing by the space in the quote.
    
    // loop over the new array, use string replace to capitlize first letter . 
    
    // take a word and captilize it 
  };

  // last left off, using a regex to pick up the first letter of each word in the array. 